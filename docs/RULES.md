# ShareGuard rule reference

Generated by `npm run docs`. Edit `src/rules.js` and regenerate instead of editing this file.

ShareGuard ships 71 rules across 3 categories. Every finding reports a file, an optional line, a rule identifier, and a severity. Matched values are never included in any output format.

Disable a rule for one scan with `--exclude-rule <id>`, restrict a scan with `--include-rule <id>` or `--category <name>`, approve a reviewed finding with an `allow` entry in `.shareguard.json`, or skip a single line with a `shareguard-ignore-line <id>` comment.

## hygiene

Delete the file, add it to `.gitignore`, or move the artifact outside the shared folder.

| Rule | Severity | Detection | Description |
| --- | --- | --- | --- |
| [`large-file`](#large-file) | medium | File size | Large file |
| [`backup-file`](#backup-file) | low | File path pattern | Backup or editor temporary file |
| [`os-metadata-file`](#os-metadata-file) | low | File path pattern | Operating system metadata file |

## privacy

Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

| Rule | Severity | Detection | Description |
| --- | --- | --- | --- |
| [`credit-card-number`](#credit-card-number) | high | Content pattern | Payment card number |
| [`us-social-security-number`](#us-social-security-number) | high | Content pattern | US Social Security number |
| [`email-address`](#email-address) | medium | Content pattern | Email address |
| [`unix-home-path`](#unix-home-path) | medium | Content pattern | Local Unix home path |
| [`windows-user-path`](#windows-user-path) | medium | Content pattern | Local Windows user path |
| [`ipv4-address`](#ipv4-address) | low | Content pattern | IPv4 address |
| [`ipv6-address`](#ipv6-address) | low | Content pattern | IPv6 address |

## secret

Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

| Rule | Severity | Detection | Description |
| --- | --- | --- | --- |
| [`anthropic-api-key`](#anthropic-api-key) | critical | Content pattern | Anthropic API key |
| [`atlassian-api-token`](#atlassian-api-token) | critical | Content pattern | Atlassian API token |
| [`aws-access-key`](#aws-access-key) | critical | Content pattern | AWS access key ID |
| [`aws-secret-access-key`](#aws-secret-access-key) | critical | Content pattern | AWS secret access key |
| [`azure-storage-key`](#azure-storage-key) | critical | Content pattern | Azure Storage account key |
| [`digitalocean-token`](#digitalocean-token) | critical | Content pattern | DigitalOcean access token |
| [`github-token`](#github-token) | critical | Content pattern | GitHub access token |
| [`gitlab-token`](#gitlab-token) | critical | Content pattern | GitLab personal access token |
| [`google-oauth-client-secret`](#google-oauth-client-secret) | critical | Content pattern | Google OAuth client secret |
| [`groq-api-key`](#groq-api-key) | critical | Content pattern | Groq API key |
| [`npm-token`](#npm-token) | critical | Content pattern | npm access token |
| [`openai-api-key`](#openai-api-key) | critical | Content pattern | OpenAI API key |
| [`openrouter-api-key`](#openrouter-api-key) | critical | Content pattern | OpenRouter API key |
| [`private-key`](#private-key) | critical | Content pattern | Private key material |
| [`private-key-file`](#private-key-file) | critical | File path pattern | Private key file |
| [`pypi-token`](#pypi-token) | critical | Content pattern | PyPI API token |
| [`sendgrid-api-key`](#sendgrid-api-key) | critical | Content pattern | SendGrid API key |
| [`slack-token`](#slack-token) | critical | Content pattern | Slack token |
| [`stripe-key`](#stripe-key) | critical | Content pattern | Stripe secret or restricted key |
| [`airtable-token`](#airtable-token) | high | Content pattern | Airtable personal access token |
| [`azure-client-secret`](#azure-client-secret) | high | Content pattern | Azure client secret |
| [`cloudflare-api-token`](#cloudflare-api-token) | high | Content pattern | Cloudflare API credential |
| [`connection-string-password`](#connection-string-password) | high | Content pattern | Password in a connection string |
| [`credential-file`](#credential-file) | high | File path pattern | Credential configuration file |
| [`discord-bot-token`](#discord-bot-token) | high | Content pattern | Discord bot token |
| [`dockerhub-token`](#dockerhub-token) | high | Content pattern | Docker Hub personal access token |
| [`doppler-token`](#doppler-token) | high | Content pattern | Doppler token |
| [`dotenv-file`](#dotenv-file) | high | File path pattern | Environment file |
| [`figma-token`](#figma-token) | high | Content pattern | Figma personal access token |
| [`generic-secret`](#generic-secret) | high | Content pattern | Hard-coded credential |
| [`google-api-key`](#google-api-key) | high | Content pattern | Google API key |
| [`grafana-cloud-token`](#grafana-cloud-token) | high | Content pattern | Grafana Cloud access policy token |
| [`grafana-service-account-token`](#grafana-service-account-token) | high | Content pattern | Grafana service account token |
| [`hashicorp-vault-token`](#hashicorp-vault-token) | high | Content pattern | HashiCorp Vault token |
| [`huggingface-token`](#huggingface-token) | high | Content pattern | Hugging Face access token |
| [`jwt`](#jwt) | high | Content pattern | JSON Web Token |
| [`linear-api-key`](#linear-api-key) | high | Content pattern | Linear API key |
| [`mailchimp-api-key`](#mailchimp-api-key) | high | Content pattern | Mailchimp API key |
| [`mailgun-api-key`](#mailgun-api-key) | high | Content pattern | Mailgun API key |
| [`new-relic-key`](#new-relic-key) | high | Content pattern | New Relic API key |
| [`notion-token`](#notion-token) | high | Content pattern | Notion integration token |
| [`perplexity-api-key`](#perplexity-api-key) | high | Content pattern | Perplexity API key |
| [`postman-api-key`](#postman-api-key) | high | Content pattern | Postman API key |
| [`replicate-api-token`](#replicate-api-token) | high | Content pattern | Replicate API token |
| [`rubygems-api-key`](#rubygems-api-key) | high | Content pattern | RubyGems API key |
| [`secret-store-file`](#secret-store-file) | high | File path pattern | Key or credential store file |
| [`service-account-key-file`](#service-account-key-file) | high | File path pattern | Service account key file |
| [`shopify-token`](#shopify-token) | high | Content pattern | Shopify access token |
| [`slack-app-token`](#slack-app-token) | high | Content pattern | Slack app-level token |
| [`sonarqube-token`](#sonarqube-token) | high | Content pattern | SonarQube token |
| [`square-access-token`](#square-access-token) | high | Content pattern | Square access token or client secret |
| [`supabase-token`](#supabase-token) | high | Content pattern | Supabase access token |
| [`telegram-bot-token`](#telegram-bot-token) | high | Content pattern | Telegram bot token |
| [`terraform-cloud-token`](#terraform-cloud-token) | high | Content pattern | Terraform Cloud API token |
| [`url-basic-auth`](#url-basic-auth) | high | Content pattern | Credential embedded in a URL |
| [`high-entropy-secret`](#high-entropy-secret) | medium | Content analysis | High-entropy credential-like value |
| [`infrastructure-state-file`](#infrastructure-state-file) | medium | File path pattern | Infrastructure state or variable file |
| [`sentry-dsn`](#sentry-dsn) | medium | Content pattern | Sentry DSN with a project key |
| [`slack-webhook-url`](#slack-webhook-url) | medium | Content pattern | Slack incoming webhook URL |
| [`twilio-api-key-sid`](#twilio-api-key-sid) | medium | Content pattern | Twilio API key SID |
| [`twilio-account-sid`](#twilio-account-sid) | low | Content pattern | Twilio account SID |

## Rule details

### anthropic-api-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Anthropic API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### atlassian-api-token

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Atlassian API token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### aws-access-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: AWS access key ID
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### aws-secret-access-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: AWS secret access key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### azure-storage-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Azure Storage account key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### digitalocean-token

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: DigitalOcean access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### github-token

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: GitHub access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### gitlab-token

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: GitLab personal access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### google-oauth-client-secret

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Google OAuth client secret
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### groq-api-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Groq API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### npm-token

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: npm access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### openai-api-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: OpenAI API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### openrouter-api-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: OpenRouter API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### private-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Private key material
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### private-key-file

- Category: `secret`
- Severity: `critical`
- Detection: File path pattern
- Description: Private key file
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### pypi-token

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: PyPI API token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### sendgrid-api-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: SendGrid API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### slack-token

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Slack token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### stripe-key

- Category: `secret`
- Severity: `critical`
- Detection: Content pattern
- Description: Stripe secret or restricted key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### airtable-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Airtable personal access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### azure-client-secret

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Azure client secret
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### cloudflare-api-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Cloudflare API credential
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### connection-string-password

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Password in a connection string
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### credential-file

- Category: `secret`
- Severity: `high`
- Detection: File path pattern
- Description: Credential configuration file
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### credit-card-number

- Category: `privacy`
- Severity: `high`
- Detection: Content pattern
- Description: Payment card number
- Resolution: Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

### discord-bot-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Discord bot token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### dockerhub-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Docker Hub personal access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### doppler-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Doppler token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### dotenv-file

- Category: `secret`
- Severity: `high`
- Detection: File path pattern
- Description: Environment file
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### figma-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Figma personal access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### generic-secret

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Hard-coded credential
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### google-api-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Google API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### grafana-cloud-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Grafana Cloud access policy token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### grafana-service-account-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Grafana service account token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### hashicorp-vault-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: HashiCorp Vault token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### huggingface-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Hugging Face access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### jwt

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: JSON Web Token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### linear-api-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Linear API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### mailchimp-api-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Mailchimp API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### mailgun-api-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Mailgun API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### new-relic-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: New Relic API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### notion-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Notion integration token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### perplexity-api-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Perplexity API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### postman-api-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Postman API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### replicate-api-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Replicate API token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### rubygems-api-key

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: RubyGems API key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### secret-store-file

- Category: `secret`
- Severity: `high`
- Detection: File path pattern
- Description: Key or credential store file
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### service-account-key-file

- Category: `secret`
- Severity: `high`
- Detection: File path pattern
- Description: Service account key file
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### shopify-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Shopify access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### slack-app-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Slack app-level token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### sonarqube-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: SonarQube token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### square-access-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Square access token or client secret
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### supabase-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Supabase access token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### telegram-bot-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Telegram bot token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### terraform-cloud-token

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Terraform Cloud API token
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### url-basic-auth

- Category: `secret`
- Severity: `high`
- Detection: Content pattern
- Description: Credential embedded in a URL
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### us-social-security-number

- Category: `privacy`
- Severity: `high`
- Detection: Content pattern
- Description: US Social Security number
- Resolution: Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

### email-address

- Category: `privacy`
- Severity: `medium`
- Detection: Content pattern
- Description: Email address
- Resolution: Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

### high-entropy-secret

- Category: `secret`
- Severity: `medium`
- Detection: Content analysis
- Description: High-entropy credential-like value
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### infrastructure-state-file

- Category: `secret`
- Severity: `medium`
- Detection: File path pattern
- Description: Infrastructure state or variable file
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### large-file

- Category: `hygiene`
- Severity: `medium`
- Detection: File size
- Description: Large file
- Resolution: Delete the file, add it to `.gitignore`, or move the artifact outside the shared folder.

### sentry-dsn

- Category: `secret`
- Severity: `medium`
- Detection: Content pattern
- Description: Sentry DSN with a project key
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### slack-webhook-url

- Category: `secret`
- Severity: `medium`
- Detection: Content pattern
- Description: Slack incoming webhook URL
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### twilio-api-key-sid

- Category: `secret`
- Severity: `medium`
- Detection: Content pattern
- Description: Twilio API key SID
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

### unix-home-path

- Category: `privacy`
- Severity: `medium`
- Detection: Content pattern
- Description: Local Unix home path
- Resolution: Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

### windows-user-path

- Category: `privacy`
- Severity: `medium`
- Detection: Content pattern
- Description: Local Windows user path
- Resolution: Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

### backup-file

- Category: `hygiene`
- Severity: `low`
- Detection: File path pattern
- Description: Backup or editor temporary file
- Resolution: Delete the file, add it to `.gitignore`, or move the artifact outside the shared folder.

### ipv4-address

- Category: `privacy`
- Severity: `low`
- Detection: Content pattern
- Description: IPv4 address
- Resolution: Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

### ipv6-address

- Category: `privacy`
- Severity: `low`
- Detection: Content pattern
- Description: IPv6 address
- Resolution: Remove or replace the personal data with a placeholder before sharing the file, or scope it to a narrow `allow` entry when it is intentional public contact information.

### os-metadata-file

- Category: `hygiene`
- Severity: `low`
- Detection: File path pattern
- Description: Operating system metadata file
- Resolution: Delete the file, add it to `.gitignore`, or move the artifact outside the shared folder.

### twilio-account-sid

- Category: `secret`
- Severity: `low`
- Detection: Content pattern
- Description: Twilio account SID
- Resolution: Remove the value from the file, rotate it with its provider, then store it in an environment variable or a secret manager.

Severity thresholds are compared with `--fail-on low|medium|high|critical|none`.
