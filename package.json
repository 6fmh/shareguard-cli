{
  "name": "shareguard-cli",
  "version": "0.1.0",
  "description": "Catch secrets and private data before you share a folder.",
  "type": "module",
  "bin": {
    "shareguard": "src/cli.js"
  },
  "scripts": {
    "check": "node --check src/cli.js && node --check src/config.js && node --check src/format.js && node --check src/ignore.js && node --check src/rules.js && node --check src/scanner.js && npm test",
    "test": "node test/index.js"
  },
  "keywords": [
    "security",
    "secrets",
    "privacy",
    "cli",
    "scanner",
    "git"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ]
}
